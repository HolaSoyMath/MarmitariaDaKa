services:
  backend:
    container_name: marmitaria-backend
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    expose:
      - "3000"
    networks:
      - shared

networks:
  shared:
    external: true
